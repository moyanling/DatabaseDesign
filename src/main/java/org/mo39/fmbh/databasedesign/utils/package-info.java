/**
 * utils package provides some utils functions.<br>
 * <p>
 * //TODO This package should be more decoupled.<br>
 * It should be separated into two package. The first package should be pure utils such as
 * DBChecker. The second package should be another layer that helps to select records, update
 * records or delete records.
 *
 */
package org.mo39.fmbh.databasedesign.utils;
